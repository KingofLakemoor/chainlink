import { fetchPlayerStat } from './src/services/propGrader.js';

async function run() {
    const config = {
      playerId: "40912",
      statType: "STRIKEOUTS",
      league: "MLB",
      teamId: "14",
      gameId: "401816234"
    };
    // we need to export fetchPlayerStat first or just copy the code
}
run();
